package bbw.narratortest;

import net.minecraft.util.Identifier;

public class NarratorNetworkConstants {
  // Save the id of the packet so we can reference it later
  public static final Identifier TTS_PACKET_ID = Identifier.of("narrator-test", "tts_payload");
  public static final Identifier EVENT_PACKET_ID = Identifier.of("narrator-test", "event_payload");
}